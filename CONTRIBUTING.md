# Contributing

Keep this repository focused on public, renderer-neutral UI foundation code.

Before sending changes:

```bash
go test ./...
```

Do not add dependencies on private `septagon-dev` repositories from this module.
